/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
    return knex.schema.table('loadouts', (table) => {
      table.integer('price').defaultTo(50)  
    })
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
    return knex.schema.table('loadouts', (table) => {
        table.dropColumn('price')
      })
};
